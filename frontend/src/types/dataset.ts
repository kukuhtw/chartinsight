// frontend/src/types/dataset.ts
export type UploadResponse = {
  uploadID: string
  columns: string[]
  rows: number
}

export type UploadMeta = {
  uploadID: string
  columns: string[]
  rows: number
}
